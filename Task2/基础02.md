# 基础02 数组，方法与内存基础

### 1.数组变量和数组对象在内存中分别表示什么

*数组变量*  存放在栈内存，只是一个地址

*数组对象* 存放在堆内存，放了很多很多元素

### 2. Java 方法参数传递特点

值传递

无论是基本数据类型还是引用数据类型，传递的都是实参的一个副本

### 3.方法重载的依据

个数不同/类型不同/顺序不同

### 4.数据越界可能出现在哪些位置

下标范围越界，比如 int a[10]

如果访问第十个数据，就发生了越界，引起报错

指针使用不当，如果没有判断好边界也会导致指针访问到其他区域

